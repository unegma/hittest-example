/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import * as THREE from 'three'
import React, { useRef } from 'react'
import { useGLTF } from '@react-three/drei'
import { GLTF } from 'three-stdlib'
const RELIC_URI = `${process.env.REACT_APP_ASSETS_URL}/pitcher-transformed.glb`;

type GLTFResult = GLTF & {
  nodes: {
    pitcher: THREE.Mesh
  }
  materials: {
    pitcher: THREE.MeshStandardMaterial
  }
}

export default function Pitcher({ ...props }: JSX.IntrinsicElements['group']) {
  const group = useRef<THREE.Group>(null)
  const { nodes, materials } = useGLTF(RELIC_URI, 'https://www.gstatic.com/draco/versioned/decoders/1.4.1/') as GLTFResult
  return (
    <group ref={group} {...props} dispose={null}>
      <mesh castShadow receiveShadow geometry={nodes.pitcher.geometry} material={materials.pitcher} />
    </group>
  )
}

useGLTF.preload(RELIC_URI)
